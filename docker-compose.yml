version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: house_api
    ports:
      - "5002:8080"
    volumes:
      - data:/app
    environment:
      - ASPNETCORE_ENVIRONMENT=Development

volumes:
  data: